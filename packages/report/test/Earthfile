VERSION 0.8
FROM node:lts
WORKDIR /workspace

deps:
  RUN mkdir node_modules
  COPY package.json package-lock.json .
  RUN npm ci
  RUN rm node_modules/@scout-for-lol/report
  COPY ../+dnt.build/npm/ node_modules/@scout-for-lol/report

test:
  FROM +deps
  COPY src src
  RUN npm run test:ci
